import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    buttonText: {
        color: "#fff",
        fontSize: 20,
        fontWeight: "500",
    },
    buttonInner: {
        width: 280,
        height: 55,
        borderRadius: 20,
        alignItems: "center",
        justifyContent: "center",
    },
  });
  